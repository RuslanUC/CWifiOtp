#ifndef COTP_MATH_UTILS_H
#define COTP_MATH_UTILS_H

long long divide(long long dividend, long long int divisor);

long long getRemainder(long long num, long long divisor);

#endif //COTP_MATH_UTILS_H
